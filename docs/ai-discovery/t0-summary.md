# PCGSOFT_AEO_T0 summary

## Observation

The first post-launch observation was recorded on 31 August 2026 against production
SHA `ca1d7c4d16256d34e998d5f75d4653c5ddb50c40`, deployment
`a4222e92-5985-490c-aaca-6d9781e68fd2`, canonical
<https://pcgsoft.co.uk/> and sitemap
<https://pcgsoft.co.uk/sitemap.xml>. The observation timestamp is
`2026-08-31T12:21:10.1128129Z` UTC / `2026-08-31T13:21:10.1128129+01:00`
Europe/London.

Pre-launch AI measurements were **NOT COLLECTED**. No model answer, citation,
mention rate or indexation state is inferred from the pre-launch documentation.

## Crawlability result

The production sitemap contained 23 URLs. All 23 returned HTTP 200 with a final
URL, title, canonical, one H1, text availability and JSON-LD present. The live
robots policy has a wildcard `Allow: /` and a sitemap declaration. This supports
anonymous retrieval and discovery; it does not prove indexing, ranking or AI
citation.

## Search result observation

The configured public web-search interface returned the PCGsoft homepage for broad
brand and site-prefix searches. Dedicated Fates, Ananke, Mnemosyne, Moirae Console,
PlainSpeak and section routes were inconsistent or absent from the observed result
sets. These are recorded as `NOT_OBSERVED` at result time and `UNKNOWN` for index
status. Search Console, Bing Webmaster Tools and IndexNow were not connected or
submitted.

## Domain observation

The apex, `www` and Pages deployment host returned 200 and pointed their canonical
identity to the apex. Classification: `CANONICAL_CORRECT` and
`REDIRECT_NORMALIZATION_NOT_IMPLEMENTED`. This is non-blocking for T0.

## Recommendation

`OBSERVATION_BASELINE_READY`

The evidence structure and repeat procedure are ready. A later operator should run
the exact query set at T+7, T+14, T+28 and T+56, then compare the five separate
metrics without creating a combined vanity score.
